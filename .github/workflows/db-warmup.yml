name: Keep API & DB Warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  db-warmup:
    runs-on: ubuntu-latest

    steps:
      - name:  Ping health DB
        run: |
          curl -X POST "${{ secrets.API_BASE_URL }}/auth/health-db" 
