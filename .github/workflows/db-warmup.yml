name: Keep API & DB Warm

on:
  schedule:
    - cron: "20 2 * * *"
  workflow_dispatch:

jobs:
  cutoff-checkout:
    runs-on: ubuntu-latest

    steps:
      - name:  Ping health DB
        run: |
          curl "${{ secrets.API_BASE_URL }}/auth/health-db" 
