name: Keep API & DB Warm

on:
  schedule:
    - cron: "15 3 * * *"
  workflow_dispatch:

jobs:
  cutoff-checkout:
    runs-on: ubuntu-latest

    steps:
      - name:  Ping health DB
        run: |
          curl -X POST "${{ secrets.API_BASE_URL }}/auth/health-db" 
